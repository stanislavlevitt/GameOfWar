import React from "react"
import "../../css/navbar.css"

const Deal = () =>{
  const startDeal = () => {

  };

  return (
    <button className="controls-btn" onClick={startDeal}>
      Draw
      </button>
  )
}

export default Deal
